{
	"name": "sample-extension",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "node build.mjs",
		"dev": "node build.mjs --watch",
		"clean": "node build.mjs --clean",
		"repo": "node repo.mjs",
		"check": "tsc --noEmit"
	},
	"devDependencies": {
		"@electron/asar": "^3.2.8",
		"@moonlight-mod/esbuild-config": "github:moonlight-mod/esbuild-config",
		"esbuild": "^0.24.2",
		"typescript": "^5.3.2"
	},
	"dependencies": {
		"@moonlight-mod/types": "^1.3.3"
	}
}
